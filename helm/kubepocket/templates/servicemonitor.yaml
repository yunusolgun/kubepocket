{{- if .Values.serviceMonitor.enabled }}
{{- $smNamespace := .Release.Namespace }}
{{- if .Values.monitoring.existingStack }}
{{- $smNamespace = .Values.monitoring.namespace }}
{{- end }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "kubepocket.fullname" . }}
  namespace: {{ $smNamespace }}
  labels:
    {{- include "kubepocket.labels" . | nindent 4 }}
    release: monitoring
    {{- with .Values.serviceMonitor.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "kubepocket.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: metrics
    path: /metrics
    interval: {{ .Values.serviceMonitor.interval }}
    scrapeTimeout: {{ .Values.serviceMonitor.scrapeTimeout }}
    {{- with .Values.serviceMonitor.metricRelabelings }}
    metricRelabelings:
      {{- toYaml . | nindent 4 }}
    {{- end }}
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}
{{- end }}